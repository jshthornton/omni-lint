[package]
name = "rule-duplicate-type"
version.workspace = true
edition.workspace = true
description = "Example omni-lint rule module: demo-gql/duplicate-type (streams findings via omni_report)"

[lib]
crate-type = ["cdylib"]

[dependencies]
omni-guest.workspace = true