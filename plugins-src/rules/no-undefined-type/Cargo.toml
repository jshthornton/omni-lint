[package]
name = "rule-no-undefined-type"
version.workspace = true
edition.workspace = true
description = "Example omni-lint rule module: demo-gql/no-undefined-type (cross-file, one file = one rule)"

[lib]
crate-type = ["cdylib"]

[dependencies]
omni-guest.workspace = true